export type TypesTodu = {
            id:number
            text: string
            data: string
        }
export type JsonData = {
   jData: [
       {
       userId:number,
       id:number,
       title:string,
       body: string
       }
   ]
}